import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Build Engine App",
  description: "Generated by the Build Engine",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}

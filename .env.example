# Default placeholders. Build Engine appends plan-required env vars below.

# Postgres connection string (set per-build by Deployer)
DATABASE_URL=

# Better Auth session secret (auto-generated per build)
BETTER_AUTH_SECRET=
